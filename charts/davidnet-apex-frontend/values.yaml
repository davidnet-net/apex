replicaCount: 1

image:
  repository: ghcr.io/davidnet-net/apex
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  host: davidnet.net
  ingressClass: traefik
